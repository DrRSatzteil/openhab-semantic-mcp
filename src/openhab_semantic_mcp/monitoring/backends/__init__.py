"""Monitoring storage backends package.

This package contains pluggable storage backends for monitoring tasks.
New backends can be added by creating a new .py file in this directory
that implements the MonitoringStorageInterface.

Available backends are automatically discovered at runtime.
"""
